import generateAccessibilityTests from '../../../../../scripts/generateAccessibilityTests';

const component = 'WalletItem';

generateAccessibilityTests({
  path: `Payment%20Wallet/${component}`,
  component,
});
